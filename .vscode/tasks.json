{
    "tasks": [
        {
            "type": "shell",
            "label": "C/C++: g++ build active file",
            "command": "/usr/bin/g++ -fdiagnostics-color=always -g ${workspaceFolder}/TutorialProject/src/*.cpp -I ${workspaceFolder}/TutorialProject/include `wx-config --cxxflags --libs` -o ${fileDirname}/${fileBasenameNoExtension}",
            "args": [],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Task generated by Debugger."
        }
    ],
    "version": "2.0.0"
}